---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: monitorgroups.uptimerobot.com
spec:
  group: uptimerobot.com
  names:
    kind: MonitorGroup
    listKind: MonitorGroupList
    plural: monitorgroups
    shortNames:
    - mgrp
    singular: monitorgroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .spec.friendlyName
      name: Name
      type: string
    - jsonPath: .status.monitorCount
      name: Count
      type: integer
    - jsonPath: .status.id
      name: ID
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MonitorGroup is the Schema for the monitorgroups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MonitorGroupSpec defines desired state of MonitorGroup.
            properties:
              account:
                description: Account references this object's Account. If not specified,
                  the default will be used.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              friendlyName:
                description: FriendlyName is the display name for the monitor group.
                maxLength: 255
                minLength: 1
                type: string
              monitors:
                description: Monitors references Monitor resources to include in this
                  group.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              prune:
                default: true
                description: Prune enables garbage collection.
                type: boolean
              pullFromGroups:
                description: PullFromGroups specifies existing group IDs to pull monitors
                  from.
                items:
                  type: integer
                type: array
              syncInterval:
                default: 24h
                description: SyncInterval defines how often the operator reconciles
                  with the UptimeRobot API.
                type: string
            required:
            - friendlyName
            type: object
          status:
            description: MonitorGroupStatus defines observed state of MonitorGroup.
            properties:
              id:
                type: string
              lastReconciled:
                format: date-time
                type: string
              monitorCount:
                type: integer
              ready:
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

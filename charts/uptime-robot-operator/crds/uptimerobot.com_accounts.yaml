---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: accounts.uptimerobot.com
spec:
  group: uptimerobot.com
  names:
    kind: Account
    listKind: AccountList
    plural: accounts
    singular: account
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .spec.isDefault
      name: Default
      type: boolean
    - jsonPath: .status.email
      name: Email
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Account is the Schema for the accounts API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccountSpec defines the desired state of Account.
            properties:
              apiKeySecretRef:
                description: ApiKeySecretRef references the secret that contains the
                  Uptime Robot API key.
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              isDefault:
                default: false
                type: boolean
            required:
            - apiKeySecretRef
            type: object
          status:
            description: AccountStatus defines the observed state of Account.
            properties:
              alertContacts:
                description: |-
                  AlertContacts lists all available alert contacts for this account.
                  Use the ID field when creating Contact resources.
                items:
                  description: AlertContactInfo represents an alert contact available
                    in UptimeRobot.
                  properties:
                    friendlyName:
                      description: FriendlyName is the display name (may be empty
                        for some contacts like email).
                      type: string
                    id:
                      description: ID is the unique identifier for this contact. Use
                        this in Contact resources.
                      type: string
                    type:
                      description: Type is the contact type (e.g., "EMAIL", "SMS",
                        "PUSHOVER").
                      type: string
                    value:
                      description: Value is the contact value (e.g., email address,
                        phone number).
                      type: string
                  required:
                  - id
                  - type
                  - value
                  type: object
                type: array
              email:
                type: string
              ready:
                type: boolean
            required:
            - email
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
